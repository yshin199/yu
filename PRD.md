# 本地文档管理桌面软件 — 产品需求文档（PRD）

版本：v1（第一版）
日期：2026-09-24
状态：已确认，作为后续开发依据

---

## 1. 产品目标和使用场景

### 产品目标
做一个运行在 Windows 电脑上的**本地桌面软件**，把「文件整理」和「随手记录」放在一个软件里，用一套分类组织起来，让个人文件不再散乱、计划和备忘有处可记。

### 使用场景
- 电脑里文件很多、散落在不同文件夹，想按自己的分类集中管理、快速找到。
- 想给文件**自动分类**（按文件类型、按文件名关键词），并能一键把它们真正整理进对应的文件夹。
- 想随手记几句备忘、列一列今天要做的计划，打开软件首页就能看到。

### 边界
- 只给个人单机使用；不联网、不登录、无云、无手机端。
- 数据只存在当前电脑，关闭/重启不丢。

---

## 2. 整体页面框架和导航结构

软件是一个桌面窗口，整体左右两栏：

- **左侧栏（固定导航）**
  - 顶部三个一级入口：**首页 / 文档管理 / 备忘笔记**。
  - 下方是**分类树**（层级分类，可展开/折叠）；点某个分类，右侧显示该分类下的文件 + 备忘。
- **右侧（内容区）**：随左侧选择变化，显示对应模块或分类的内容。

导航规则：
- 首页是「总览」，点击任意模块入口或分类可进入详情。
- 首页里的今日计划、快速备忘、摘要都来自两个模块的数据，点击可跳转到对应位置。

---

## 3. 首页的内容结构

首页只做「看」和「快速记」，从上到下三块：

1. **今日计划**：今天的计划列表（来自备忘模块），每条前有勾选框，点一下即完成；顶部输入框回车即新增一条。
2. **快速备忘**：显示最近几条快速备忘，可一键新增。
3. **各模块摘要**：
   - 文档管理：文件总数、待分类文件数、最近添加的文件。
   - 备忘笔记：未完成的今日计划数、备忘总数、最近备忘。

---

## 4. 每个模块的用途

- **首页**：总览入口，快速看到今天的计划、最近的备忘，以及两个模块的关键统计；不存储自己的数据。
- **文档/文件管理**：登记、分类、整理电脑里的真实文件。
- **备忘/笔记**：随手记录文字备忘，管理今日计划。

---

## 5. 每个模块第一版最必要的数据和操作

### 文档/文件管理
- **数据（文件条目）**：文件名、原路径、文件类型（按扩展名推断）、所属分类、备注、大小、登记时间、状态（是否已真正归入文件夹）。
- **操作**：
  - 登记文件（单选/多选文件，或扫描整个文件夹，含子文件夹）。
  - 手动分类（拖拽或右键把文件归到某分类）。
  - 自动分类（按文件类型 + 文件名关键词，预览后确认）。
  - 一键归入文件夹（把分类下的文件真实移动到对应文件夹）。
  - 打开原文件（双击用系统默认程序打开）。
  - 搜索 / 筛选 / 排序。
  - 编辑备注。

### 备忘/笔记
- **数据（备忘）**：标题、内容、类型（普通备忘 / 快速备忘 / 今日计划）、所属分类（可空）、置顶、完成状态、归档、计划日期、创建/修改时间。
- **操作**：
  - 新建 / 编辑 / 删除备忘。
  - 快速备忘（一句话，不强制标题）。
  - 今日计划（勾选完成，完成后归入「已完成」）。
  - 归到某分类、置顶。
  - 搜索、归档。

---

## 6. 首页、今日计划和各模块之间的关系

- **首页是「汇总视图」**：不存自己的数据，它显示的今日计划、快速备忘、摘要都实时来自两个模块。
- **今日计划是「备忘/笔记」模块里的一种类型**（type=今日计划）：本质是一条带日期、可勾选完成的备忘。在备忘模块里新增/勾选，首页同步显示。
- **各模块摘要** = 对两个模块数据的实时统计（文件总数、待分类数、备忘总数、未完成计划数等）。
- **分类是共享的**：文档和备忘共用一套分类树，点某个分类能同时看到该分类下的文件和备忘。
- **数据流**：两个模块 → 本地存储（SQLite）→ 首页按需统计展示；首页的勾选/新增操作写回备忘模块。

---

## 7. 本地数据文件、备份和恢复要求

- **数据文件**：所有数据（分类、文件条目、备忘、自动分类规则）存在一个本地的 SQLite 数据库文件里，路径固定，可在「设置」里查看。
- **自动保存**：任何增删改操作后立即保存，无需手动「保存」；关闭、重启软件数据不丢。
- **备份**：提供「导出备份」功能，把当前数据打包成一个备份文件（可复制到 U 盘或其他地方保存）。
- **恢复**：提供「从备份恢复」功能，选择备份文件后恢复数据（恢复前提示会覆盖当前数据）。
- **第一版不做**：自动定时备份、云备份。

---

## 8. 第一版必须完成的功能

- Windows 桌面程序（Electron 打包成 .exe），双击打开独立窗口，不经过浏览器。
- SQLite 本地存储，数据重启不丢。
- 首页：今日计划、快速备忘、两个模块摘要。
- 文档管理：登记文件、层级分类（增删改 + 子分类）、手动分类、自动分类（类型 + 关键词，预览 + 撤回）、一键归入真实文件夹、打开原文件、搜索筛选、备注。
- 备忘笔记：普通备忘、快速备忘、今日计划（勾选完成）、分类、置顶、搜索、归档。
- 自定义分类名称、层级子分类。
- 导出备份 / 从备份恢复。

---

## 9. 第一版暂时不做的功能

- 多标签（一个东西属于多个分类）。
- 按日期 / 来源的自动分类。
- 内容识别（读取文件内容、OCR）。
- 备忘富文本、把文件作为附件挂到备忘上、提醒/通知。
- 云同步、登录、手机端。
- 自动定时备份、云备份。
- 文件历史版本、回收站。

---

## 10. 可以实际检查的验收标准

以下每条都可独立检查，作为开发是否完成的判定依据。

### A. 安装与启动
1. 双击安装程序，能在 Windows 上完成安装。
2. 安装后桌面或开始菜单出现软件图标，双击能打开独立窗口（不是浏览器）。
3. 断网状态下，软件能正常打开和使用。

### B. 数据与持久化
4. 新建分类、文件、备忘后，关闭软件再打开，内容不丢。
5. 重启电脑后再打开软件，内容不丢。
6. 在「设置」里能看到数据文件的存储路径。

### C. 分类
7. 能新建分类，分类名可自定义（中文、英文均可）。
8. 能重命名分类、删除分类。
9. 能在分类下新建子分类，子分类显示在父分类下，可展开/折叠。
10. 文档和备忘共用同一套分类树。

### D. 文档/文件管理
11. 能登记单个文件；能扫描整个文件夹（含子文件夹）批量登记。
12. 登记后，文件在电脑里的原始位置不变（不被移动或删除）。
13. 文件列表能显示文件名、类型、分类、大小、日期。
14. 能把文件手动归到某个分类。
15. 新登记的文件默认「未分类」，并计入首页的「待分类文件数」。
16. 按文件类型自动分类：PDF 建议归「文档」、图片建议归「图片」等（可预览）。
17. 按文件名关键词自动分类：设置「文件名含『合同』→ 归到『合同』分类」的规则后，能正确匹配文件名含「合同」的文件。
18. 自动分类执行前能看到预览清单，可勾选/调整，确认后才生效。
19. 「一键归入文件夹」能把分类下的文件真实移动到目标文件夹（原位置消失、目标文件夹出现）。
20. 双击文件条目，能用系统默认程序打开原文件。
21. 能按关键词搜索文件，能按类型/分类筛选。

### E. 备忘/笔记
22. 能新建普通备忘（标题 + 正文）。
23. 能新建快速备忘（一句话，不强制标题）。
24. 能新建今日计划，勾选「完成」后从「今日」移入「已完成」。
25. 备忘能归到某个分类、能置顶。
26. 能按关键词搜索备忘。
27. 备忘能归档，归档后从主列表隐藏但可找回。

### F. 首页
28. 首页显示今日计划，勾选状态与备忘模块同步。
29. 首页显示最近快速备忘。
30. 首页显示文档管理摘要（文件总数、待分类数、最近添加）。
31. 首页显示备忘摘要（未完成今日计划数、备忘总数）。
32. 首页能直接新增今日计划和快速备忘。

### G. 备份与恢复
33. 能「导出备份」，生成一个可复制到别处的备份文件。
34. 能「从备份恢复」，恢复后数据与备份时一致。

---

> 本文档合并了已确认的产品范围与页面/交互方案；第 10 节验收标准可逐条检查，作为开发完成与否的判定依据。
